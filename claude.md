# 病院予約確認システム(職員用フロントエンド)

## 概要
病院職員が予約状況・患者情報・医師情報を確認するための社内向け管理画面。
**患者(顧客)向けのページは作らない。** 全ページは病院職員のみが利用する前提。

## 対象ユーザー
- 病院の受付・看護師・事務職員などのスタッフ
- 患者本人は利用しない(患者向けUI・患者向け予約フローは実装しない)

## 技術スタック
- React + TypeScript + Vite
- ルーティング: react-router-dom
- スタイリング: 未指定(実装時に決定。CSS Modules or Tailwind等、シンプルなもので可)
- データ: 現段階ではモックデータ(ダミーJSON)を使用し、API連携は行わない
  - `src/mocks/` にダミーデータを配置し、将来的に実APIへ差し替えやすい形(fetch層を関数で抽象化)にする

## 国際化(i18n)
本システムは国際向けであり、以下の言語をユーザーがいつでも切り替えられるようにする。

- 対応言語: 日本語(ja) / 韓国語(ko) / 台湾語・繁体字中国語(zh-TW)
- 全ページ共通のヘッダー等に言語切り替えUI(セレクタ)を配置し、画面遷移せずに即時切り替えできるようにする
- 選択した言語は再読み込み後も保持する(ブラウザのlocalStorage等に保存)
- デフォルト言語は日本語(ja)とする
- 実装: react-i18next + i18next を使用
  - `src/locales/ja.json`, `src/locales/ko.json`, `src/locales/zh-TW.json` に翻訳キーをまとめる
  - 画面上の文言はハードコーディングせず、必ず翻訳キー経由で表示する
  - 日付・時刻の表示形式も言語/地域に応じて整形する(Intl.DateTimeFormat等を利用)

## 認証
- ログイン画面を用意する(職員IDまたはメールアドレス + パスワードでの認証を想定)
- 未ログイン状態では各ページにアクセスできないようにする(ルートガード)
- 認証も現段階ではモック実装で良い(固定ユーザーでのログイン成功/失敗をシミュレート)

## ページ構成
ログイン画面 + 以下4つの機能ページで構成する。

### 1. Main(当日スケジュール)
- 本日の予約一覧をスケジュール形式で表示
- 表示項目例: 時間、患者名、担当医、診療科、予約状況(受付済み/診察中/完了/キャンセル等)
- 職員が「今日、誰が何時に来るか」を一目で把握できることが目的

### 2. All in One(週次・年次予約 / 医師情報)
- 週単位・年単位で予約状況を確認できるカレンダー/一覧ビュー
- 医師ごとのスケジュール・担当診療科・稼働状況などの医師情報を確認できる
- 週表示・年表示の切り替え、医師フィルタなどを想定

### 3. Notification(予約履歴ログ)
- 予約の変更履歴(作成・変更・キャンセルなど)をログ形式で時系列に表示
- 表示項目例: 発生日時、操作種別(新規予約/変更/キャンセル)、対象患者、対象予約、操作者
- 「いつ誰が何をしたか」を追跡できることが目的

### 4. Patient Info(患者情報リスト)
- 患者情報をリスト形式で表示
- 表示項目例: 患者名、患者ID、生年月日、連絡先、直近の受診歴、担当医など
- 検索・絞り込みができるとよい(将来拡張)

## ディレクトリ構成(想定)
```
src/
  pages/
    Login.tsx
    Main.tsx
    AllInOne.tsx
    Notification.tsx
    PatientInfo.tsx
  components/       # 各ページで使う共通UIパーツ
  mocks/            # ダミーデータ(予約、医師、患者、ログ)
  locales/          # 翻訳ファイル(ja.json / ko.json / zh-TW.json)
  types/            # 型定義(Reservation, Doctor, Patient, LogEntry など)
  routes/           # ルーティング定義・認証ガード
  App.tsx
  main.tsx
```

## 実装方針
- まずモックデータでUIを完成させ、データ取得部分は関数で抽象化しておく(後でAPI呼び出しに差し替え可能にする)
- 患者向け機能(オンライン予約、患者ログインなど)は範囲外。実装しない
- 過度な抽象化は避け、4ページ+ログインというシンプルな構成を保つ
